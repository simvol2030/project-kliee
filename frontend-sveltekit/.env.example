# Session Secret for encrypting session cookies
# Generate a secure random secret using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# IMPORTANT: Keep this secret safe and never commit the actual .env file to version control
SESSION_SECRET=your-random-32-byte-base64-encoded-secret-here

# Node Environment (development | production)
NODE_ENV=development

# Database
DATABASE_TYPE=sqlite

# Public Base URL (for Stripe callbacks, OG images, etc.)
PUBLIC_BASE_URL=http://localhost:5173

# Stripe Payment Integration (optional - enables Stripe Checkout)
# Get keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# Get webhook secret from https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# SHOP MODULE - Email Notifications (SMTP)
# ===========================================
# SMTP server for sending order notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password

# Admin email for receiving order notifications
SHOP_ADMIN_EMAIL=admin@example.com

# ===========================================
# SHOP MODULE - Telegram Notifications
# ===========================================
# Create bot via @BotFather and get token
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather

# Get chat ID by adding bot to group and calling getUpdates API
# https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_CHAT_ID=your-group-chat-id
