# Database
# Use absolute path for WSL2 compatibility
DATABASE_URL="file:/mnt/c/dev/box-app/project-box-combo/data/db/app.db"
# Or relative path (may have issues on WSL2):
# DATABASE_URL="file:../data/db/app.db"

# Session Secret (CHANGE IN PRODUCTION!)
SESSION_SECRET="your-secret-key-change-me-in-production"

# Environment
NODE_ENV="development"

# Server Port (optional, defaults to 5173)
PORT=5173
