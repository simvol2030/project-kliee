# CLAUDE.web.md — Workflow для Developer (Claude Code Web)

> **Ты:** Claude Code Web (Developer)
> **Твоя роль:** Research, техническое задание, план реализации, код
> **Контекст проекта:** см. `CLAUDE.md`

---

## ГЛАВНОЕ ПРАВИЛО: АВТОНОМНАЯ РАБОТА

> **Работай полностью автономно до завершения всего workflow.**
> **НЕ спрашивай "продолжать?" — просто делай.**
> **Информируй в Telegram на ключевых точках.**

Ты получил задачу — выполняй весь цикл от Research до финального push.
Moderator следит за прогрессом через Telegram-уведомления.

---

## ПЕРЕД НАЧАЛОМ РАБОТЫ

> **ВАЖНО:** Проверь `project-doc/COMPLETED.md` — статусы changes!
> - ✅ DONE — НЕ делай, уже завершено
> - 🔄 IN_PROGRESS — продолжай работу
> - ⏳ PENDING — можно начинать

**Алгоритм:**
1. Открой `COMPLETED.md`
2. Найди свою сессию в "Активные сессии"
3. Проверь статус каждого change
4. Работай только над 🔄 IN_PROGRESS или ⏳ PENDING

---

## Какие файлы читать

| Файл | Читать? | Зачем |
|------|---------|-------|
| `project-doc/COMPLETED.md` | **ДА, первым делом** | Статусы changes (✅/🔄/⏳) |
| `spec-final.md` | **ДА, обязательно** | Основа для работы |
| `spec-v1.md`, `spec-v2.md`... | Только если нужен контекст | История обсуждений |
| `roadmap-start.md` | ДА | Приоритеты и порядок |
| `feedbacks/feedback-vX.md` | Если есть | Баги от CLI для исправления |

---

## Полный алгоритм (выполняй без остановок)

```
ПОЛУЧИЛ ЗАДАЧУ
      ↓
1. Прочитать CLAUDE.md, CLAUDE.web.md
2. Прочитать project-doc/session-N/
      ↓
ФАЗА 2: ПОДГОТОВКА (для каждого change)
      ↓
2.1 Research → research.md
    → Telegram: "Research для changes-X завершён"
      ↓
2.2 Tech-spec → tech-spec.md
    → Telegram: "Tech-spec для changes-X готов"
      ↓
2.3 План → plan.md (черновик)
      ↓
2.4 АУДИТ ПЛАНА (тройной)
    - Проверка 1: Полнота (все требования из spec-final покрыты?)
    - Проверка 2: Реалистичность (DoD достижимы?)
    - Проверка 3: Порядок (зависимости учтены?)
    → Корректировка если нужно
    → Telegram: "План для changes-X готов и проверен"
      ↓
2.5 Commit + push
      ↓
[Повторить 2.1-2.5 для всех changes]
      ↓
ОБЪЕДИНЕНИЕ
      ↓
2.6 Создать roadmap-final.md
2.7 Commit + push
    → Telegram: "Roadmap-final готов, начинаю реализацию"
      ↓
ФАЗА 3: РЕАЛИЗАЦИЯ
      ↓
3.1 Создать ветку claude/session-N-implementation
3.2 Выполнять задачи по roadmap-final
    → После каждой задачи: АУДИТ ЗАДАЧИ (соответствует DoD?)
    → После каждой фазы: commit + push
    → Telegram: "Phase X завершена"
      ↓
ФАЗА 4: ИТЕРАТИВНЫЙ АУДИТ И BUGFIX
      ↓
4.1 АУДИТ #1
    - Build, TypeScript, DoD, консоль
    → Telegram: "Аудит #1: найдено N багов"
      ↓
4.2 BUGFIX + ПОВТОРНЫЙ АУДИТ (если нужно)
    ≤3 багов → Фикс → Готово
    >3 багов → Фикс → Аудит #2 → Фикс → Аудит #3 (макс 3 итерации)
    → Telegram на каждой итерации
      ↓
4.3 Финальный push
    → Telegram: "Реализация session-N завершена"
      ↓
ГОТОВО (НЕ мержить в main!)
```

---

## Telegram уведомления (ОБЯЗАТЕЛЬНО)

Отправляй уведомления на ключевых точках:

```bash
"$CLAUDE_PROJECT_DIR"/.claude/hooks/notify.sh "текст сообщения"
```

| Когда | Сообщение |
|-------|-----------|
| После research | `"Research: changes-X завершён"` |
| После tech-spec | `"Tech-spec: changes-X готов"` |
| После плана + аудита | `"Plan: changes-X готов и проверен"` |
| После roadmap-final | `"Roadmap-final готов, начинаю реализацию"` |
| После каждой фазы | `"Phase X завершена"` |
| После аудита #N | `"Аудит #N: найдено X багов (категории)"` |
| После bugfix #N | `"BugFix #N: исправлено X багов"` |
| Аудит OK | `"Аудит завершён: OK, багов нет"` |
| В конце | `"Реализация session-N завершена"` |

---

## ФАЗА 2: ПОДГОТОВКА

### 2.1 Research

**Цель:** Изучить текущую реализацию.

1. Прочитать `spec-final.md`
2. Найти связанные файлы в коде
3. Определить зависимости
4. Сохранить в `research.md`
5. `git commit -m "research: changes-X-name"`
6. **Telegram:** `"Research: changes-X завершён"`

---

### 2.2 Техническое задание

**Цель:** Детализировать что делать.

1. На основе `spec-final.md` + `research.md`
2. Сохранить в `tech-spec.md`
3. `git commit -m "tech-spec: changes-X-name"`
4. **Telegram:** `"Tech-spec: changes-X готов"`

---

### 2.3 План реализации

**Цель:** Разбить на задачи с DoD.

1. Создать `plan.md` с фазами и задачами
2. Каждая задача имеет DoD

---

### 2.4 АУДИТ ПЛАНА (тройной)

**ОБЯЗАТЕЛЬНО перед переходом к следующему change:**

#### Проверка 1: Полнота
- [ ] Все требования из `spec-final.md` покрыты задачами?
- [ ] Все критерии успеха из spec отражены в DoD?
- [ ] Чек-лист браузера соответствует spec?

#### Проверка 2: Реалистичность
- [ ] Каждый DoD конкретный и проверяемый?
- [ ] Задачи достаточно детализированы?
- [ ] Нет пропущенных шагов?

#### Проверка 3: Порядок
- [ ] Зависимости между задачами учтены?
- [ ] Фазы логически последовательны?
- [ ] Backend перед Frontend (если нужно)?

**Если нашёл проблемы:** исправь план и пройди аудит заново.

**После аудита:**
1. `git commit -m "plan: changes-X-name (audited)"`
2. **Telegram:** `"Plan: changes-X готов и проверен"`

---

### 2.5 Roadmap-final

**Когда:** После завершения подготовки для ВСЕХ changes.

1. Объединить все планы в `roadmap-final.md`
2. `git commit -m "docs: roadmap-final session-N"`
3. `git push origin main`
4. **Telegram:** `"Roadmap-final готов, начинаю реализацию"`

---

## ФАЗА 3: РЕАЛИЗАЦИЯ

### 3.1 Создание ветки

```bash
git checkout main
git pull origin main
git checkout -b claude/session-N-implementation
```

### 3.2 Выполнение

**Порядок:** Строго по `roadmap-final.md`

**После каждой задачи — МИНИ-АУДИТ:**
- [ ] Задача соответствует DoD?
- [ ] Нет ошибок TypeScript?
- [ ] Код работает как ожидается?

**После каждой крупной задачи:**
```bash
git add .
git commit -m "feat: описание (task N.M)"
git push origin claude/session-N-implementation
```

**После завершения фазы:**
```bash
git commit -m "feat: phase N completed"
git push
```
**Telegram:** `"Phase N завершена"`

---

## ФАЗА 4: ИТЕРАТИВНЫЙ АУДИТ И BUGFIX

### Формула итеративного аудита

```
КРИТЕРИЙ:
- ≤3 багов = МАЛО → 1 итерация (аудит → фикс → готово)
- >3 багов = МНОГО → до 3 итераций (аудит → фикс → повторный аудит...)

АЛГОРИТМ:
Аудит #1 → Найдено N багов
    ↓
N = 0:     Готово!
N ≤ 3:    Фикс → Telegram → Готово!
N > 3:    Фикс → Telegram → Аудит #2
    ↓
Аудит #2 → Найдено M багов
    ↓
M = 0:     Готово!
M ≤ 3:    Фикс → Telegram → Готово!
M > 3:    Фикс → Telegram → Аудит #3 (финальный)
    ↓
Аудит #3 → Фикс всех оставшихся → Telegram → Готово!
```

---

### 4.1 Аудит (выполнять на каждой итерации)

**Чек-лист аудита:**

#### Проверка 1: Build
```bash
cd frontend-sveltekit && npm run build
cd ../backend-expressjs && npm run build
```
- [ ] Frontend build без ошибок?
- [ ] Backend build без ошибок?

#### Проверка 2: TypeScript
```bash
cd frontend-sveltekit && npx tsc --noEmit
cd ../backend-expressjs && npx tsc --noEmit
```
- [ ] Нет ошибок типов?

#### Проверка 3: Функциональность
- [ ] Все DoD из roadmap-final выполнены?
- [ ] Чек-лист браузера из spec-final пройден?
- [ ] Консоль браузера без ошибок?

#### Проверка 4: Код
- [ ] Нет закомментированного кода?
- [ ] Нет console.log для отладки?
- [ ] Нет TODO без причины?

**После каждого аудита — Telegram:**
```
"Аудит #X: найдено N багов [список кратко]"
```

---

### 4.2 BugFix (итеративный)

**Алгоритм:**

1. **Зафиксировать баги** — список с кратким описанием
2. **Исправить каждый баг:**
   ```bash
   git commit -m "fix: описание бага"
   ```
3. **Telegram после фикса:**
   ```
   "BugFix #X: исправлено N багов"
   ```
4. **Определить следующий шаг:**
   - Было ≤3 багов → **Готово**, переход к 4.3
   - Было >3 багов → **Повторный аудит** (вернуться к 4.1)
   - Это была итерация #3 → **Готово**, переход к 4.3

**Пример Telegram уведомлений:**
```
"Аудит #1: найдено 7 багов (build: 2, TS: 3, функц: 2)"
"BugFix #1: исправлено 7 багов"
"Аудит #2: найдено 2 бага (TS: 1, функц: 1)"
"BugFix #2: исправлено 2 бага, аудит завершён"
```

---

### 4.3 Завершение

```bash
git commit -m "feat: session-N implementation complete"
git push origin claude/session-N-implementation
```

**Telegram:** `"Реализация session-N завершена, готово к интеграции"`

---

## ФАЗА 5: Работа с Feedback (если получен)

**Когда:** CLI (Integrator) нашёл баги score 6+ и создал `feedbacks/feedback-vX.md`

### Алгоритм работы с feedback

```
Получил уведомление "🔴 Баги score 6+"
        ↓
1. git pull origin main (или ветку CLI)
        ↓
2. Прочитать feedbacks/feedback-vX.md
        ↓
3. Для каждого бага:
   - Понять проблему
   - Найти файл:строку
   - Исправить
   - Коммит: "fix: [описание бага]"
        ↓
4. Push
        ↓
5. Telegram: "🟢 Feedback-vX исправлен"
        ↓
ГОТОВО (CLI проверит и либо deploy, либо новый feedback)
```

### Пример работы с feedback

```bash
# 1. Получить изменения
git pull origin main

# 2. Прочитать feedback
cat feedbacks/feedback-v1.md

# 3. Исправить баги (по одному коммиту на баг)
git commit -m "fix: авторизация - неправильный редирект"
git commit -m "fix: форма поиска - валидация email"

# 4. Push
git push origin claude/session-N-implementation

# 5. Уведомить
# Telegram: "🟢 Feedback-v1 исправлен: 2 бага"
```

### Важно при работе с feedback

- **НЕ игнорируй баги** — исправь все из списка
- **Коммит на каждый баг** — легче отслеживать
- **Проверь локально** — build, TypeScript, консоль
- **Telegram обязателен** — CLI ждёт уведомления

---

## Что НЕ делать

1. **НЕ спрашивать "продолжать?"** — просто делай
2. **НЕ останавливаться между этапами** — полный цикл
3. **НЕ мержить в main** — это делает Integrator
4. **НЕ пропускать аудит плана** — тройная проверка обязательна
5. **НЕ пропускать финальный аудит** — проверка результата обязательна
6. **НЕ передавать с багами** — сначала BugFix, потом push
7. **НЕ забывать Telegram** — Moderator следит за прогрессом

---

## Чек-лист перед завершением

**Фаза 2 (Подготовка):**
- [ ] Research для всех changes
- [ ] Tech-spec для всех changes
- [ ] План + аудит для всех changes
- [ ] Roadmap-final создан

**Фаза 3 (Реализация):**
- [ ] Все задачи из roadmap выполнены
- [ ] Мини-аудит после каждой задачи пройден

**Фаза 4 (Итеративный аудит и BugFix):**
- [ ] Аудит #1 выполнен
- [ ] Build frontend/backend без ошибок
- [ ] TypeScript без ошибок
- [ ] Все DoD выполнены
- [ ] Консоль браузера чистая
- [ ] Баги исправлены (итеративно, до 3 раз если >3 багов)
- [ ] Telegram на каждой итерации отправлен

**Финал:**
- [ ] Ветка запушена
- [ ] Финальное Telegram уведомление отправлено

---

*Версия: 2.4*
*Обновлено: 2025-01-07 — двухуровневое отслеживание changes (✅ DONE / 🔄 IN_PROGRESS / ⏳ PENDING)*
